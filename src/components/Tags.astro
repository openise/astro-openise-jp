---
const { post } = Astro.props;
---
{post.data.tags.map((tag: string) => (
  <p class="animate text-base m-1 p-2 rounded-lg">
    <a class="hover:underline" href={`/astro-openise-jp/tags/${tag}`}>{tag}
    </a>
  </p>
))}